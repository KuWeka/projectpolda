# Root environment reference
# This repo uses service-specific env files:
# - backend/.env.example
# - apps/web/.env.example

# Optional local overrides for root scripts (if needed)
BACKEND_PORT=3001
WEB_PORT=3000
